#!/usr/bin/env bash

# This script guides you through focused work on important things using a custom inner routine for firstlife.

. $SKIGGETY_UTILS_DIR/lib/skiggety-utils.bash || exit 1
. $SKIGGETY_UTILS_DIR/lib/firstlife.bash || exit_with_error 'could not use common firstlife code'

# TODO^5?: exit if a script with identical name and path is in parent tree

exit_if_day_is_over

# This calls $SKIGGETY_UTILS_DIR/bin/templates/firstlife-focus.routine.TEMPLATE based on the name of this calling script:
use_and_maintain_inner_routine_based_on_template 'focus' "$0"

exit 0
