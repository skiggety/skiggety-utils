#!/usr/bin/env bash

# This script defines your main activities to be productive during the day

. $SKIGGETY_UTILS_DIR/lib/skiggety-utils.bash || exit 1
. $SKIGGETY_UTILS_DIR/lib/firstlife.bash || exit_with_error 'could not use common firstlife code'

# TODO^4?: exit if a script with identical name and path is in parent tree

exit_if_day_is_over

use_and_maintain_inner_routine_based_on_template 'work' "$0"

exit
