#!/usr/bin/env bash

# This script does things to wake you up

. $SKIGGETY_UTILS_DIR/lib/skiggety-utils.bash || exit 1
. $SKIGGETY_UTILS_DIR/lib/firstlife.bash || exit_with_error 'could not use common firstlife code'

firstlife-alarm "Welcome to today's firstlife terminal, please pay attention" && debug_here foo || debug_here bar
exit_if_day_is_over

handle-alarmy-photo-mission || accumulate_error "failed to handle alarm" # for 'Alarmy'
exit_if_day_is_over

exit 0
