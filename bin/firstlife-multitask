#!/usr/bin/env bash

# This program uses shellask to prompt you to do a tolerable mix of productive stuff. It's good to use in moderation and
# as a warm-up for focused work.

. $SKIGGETY_UTILS_DIR/lib/skiggety-utils.bash || exit 1
. $SKIGGETY_UTILS_DIR/lib/firstlife.bash || exit_with_error 'could not use common firstlife code'

# TODO^3?: exit if a script with identical name and path is in parent tree

exit_if_day_is_over

# This calls $SKIGGETY_UTILS_DIR/bin/templates/firstlife-multitask.routine.TEMPLATE based on the name of this calling script:
use_and_maintain_inner_routine_based_on_template 'multitask' "$0"

exit
