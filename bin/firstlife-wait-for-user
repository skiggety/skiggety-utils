#!/usr/bin/env bash

# This script waits for the user to show readiness to interact.  It's a useful building block in other scripts.

. $SKIGGETY_UTILS_DIR/lib/skiggety-utils.bash || exit 1
. $FIRSTLIFE_CODE_DIR/lib/firstlife.bash || exit_with_error "could not source firstlife.bash"

# TODO^95: use liberally at the beginning of crontab lines or scripts that are usually used with cron

# TODO^33: this should should display what's waiting and since when
# - TODO^34: do I need to take a message input from $* or $@ ?

message="Press any key to continue"
if $WISEASS_MODE; then
    message="Press the 'any' key to continue, if you can find it."
fi
read -n 1 -s -r -p "$message"
debug_here
exit 0
