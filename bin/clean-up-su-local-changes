#!/usr/bin/env bash

# This script helps the user to make sure changes to skiggety-utils get committed and pushed

. $SKIGGETY_UTILS_DIR/lib/skiggety-utils.bash || exit 1

# TODO^5: make another one for firstlife-repo instead of su

cd $SKIGGETY_UTILS_DIR

if git-no-unpushed-changes && git-no-uncommitted-changes; then
    exit 0
else
    dashboard
    firstlife-delegate --with-reward 'commit something, probably using "ci && git ready && yup".'
fi
