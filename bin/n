#!/usr/bin/env bash

# This script exits with 1, for when the alias unpredictably fails..yeah, it's a hack.  There is also 'y', 'yup', and 'nope

. $SKIGGETY_UTILS_DIR/lib/skiggety-utils.bash || exit 1

exit 1
