#!/usr/bin/env bash

# This script adds a file from the path

. $SKIGGETY_UTILS_DIR/lib/skiggety-utils.bash || exit 1

# TODO^21: IMPLEMENT for real, parse args, feed them to git, then do which on what's left"

target="$(which $1)"
cd "$(dirname $target)"
git add -N $target
git add -p $target
