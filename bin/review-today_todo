#!/usr/bin/env bash

# This script shows a continually updated todo list report

# TODO^21: (IN_PROGRESS, EASY, NOW, and dont forget TESTING) gotta RENAME this and today_todo.  Underscores in the name are just too weird, especially when mixed with hyphens

. $SKIGGETY_UTILS_DIR/lib/vital.bash || exit_with_error 'could not include vital.bash'

cd $VITAL_DIR # so 'review' watches the right part of the disk:
review -u 10 -m 180 vital --report
