#!/usr/bin/env bash

# This script shows status the way I usually like to see it, without overriding the often useful 'git status' command.
# btw, remember you can override it with your PATH order (e.g. '~/bin', '..path/to/skiggety-utils/PWD_BIN/')

. $SKIGGETY_UTILS_DIR/lib/skiggety-utils.bash || exit 1

git -c color.ui=always --no-pager status -ss
