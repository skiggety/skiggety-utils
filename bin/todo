#!/usr/bin/env python

import sys
import os

# TODO TODO TODO TODO TODO: figure out how to run this with the right python version even if you're running it in another directory
if sys.version_info[0] < 3:
    raise Exception('Must be using Python 3')

sys.path.append(os.path.abspath(os.path.join(os.getenv('SKIGGETY_UTILS_DIR'),"lib")))
from todo_line import *

# call old implementation
os.system("todo-old")
