# This should be included into the user's .bashrc file)

# ./PWD_BIN comes before $PATH_TO_SKIGGETY_UTILS/bin so that you can override scripts like "dashboard"
export PATH="$PATH:./PWD_BIN:$PATH_TO_SKIGGETY_UTILS/bin"

export PS1="\$(duh)
$ "

# TODO run 'ssh-agent' in background?

export CLICOLOR='xterm-color' # colorize ls output and such in mac xterm

# TODO TODO TODO: use stuff from older bashrc files I've used
