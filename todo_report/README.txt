==== TODOs, ascending, so you know what's likely to change next. Do not edit, run 'generate_todo_report' to generate. ====
( showing last 8 lines out of 814 for 'todo': )
./README-firstlife.md:9:  - TODO^114: move firstlife to 'zerothlife' repo and rename all mentions of 'firstlife' either to 'zerothlife' or 'zl'
./installers/zerothlife:27:    false # TODO^114: after install is implemented, call 'firstlife-setup'
./installers/zerothlife:35:    # TODO^115: (IN_PROGRESS, NOW) and add the new repo's bin dir to your path
./README.md:126:  - TODO^116: move 'vital' data (to do list) to private repo (skiggety utils should help set up)
./bin/firstlife-wait-for-routine:18:cd $SKIGGETY_UTILS_DIR && shellask "Ready to work on $(basename $0) just a little before we wait for the '$WHICH_ROUTINE' routine?" && vim $0 # TODO^116: (IN_PROGRESS) DELETE once IMPLEMENTATION works
./bin/firstlife-setup:11:# TODO^117: generate example crontab file, (it can start as an empty stub) including:
./bin/firstlife-status:68:# TODO^152: similarly, show current alarms, especially if muted!!
./bin/firstlife-wait-for-routine:9:# EASY TODO^155: (TESTING IN_PROGRESS) can we wait for different routines successfully? (having the evening routine wait for the work routine right now...)
